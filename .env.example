# Server-side only. Never prefix provider keys with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional future provider adapters. Leave unset until the adapter is enabled.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
